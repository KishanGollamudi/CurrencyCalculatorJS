name: CI-CD Pipeline

on:
  push:
    branches: ["main"]

jobs:
  build-test-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout Repo
      uses: actions/checkout@v3

    - name: 🟦 Setup Node
      uses: actions/setup-node@v3
      with:
        node-version: "20"

    - name: 📦 Install Dependencies
      run: npm install

    - name: 🧪 Run tests
      run: npm test

    - name: 🔐 Login to DockerHub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: 🐳 Build Docker image
      run: docker build -t ${{ secrets.DOCKER_USERNAME }}/currencycalculatorjs:latest .

    - name: 🚀 Push Docker image
      run: docker push ${{ secrets.DOCKER_USERNAME }}/currencycalculatorjs:latest

